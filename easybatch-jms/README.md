# easybatch-jms

Easy Batch - JMS integration module

Child of `com.billing.easybatch:easybatch`.

## Maven coordinates

| Field | Value |
|---|---|
| groupId | `com.billing.easybatch` |
| artifactId | `easybatch-jms` |
| version | `5.3.0-jakarta` |
| packaging | `jar` |

## Build

`mvn install -pl 
