# easybatch-jdbc

Easy Batch jdbc module

Child of `com.billing.easybatch:easybatch`.

## Maven coordinates

| Field | Value |
|---|---|
| groupId | `com.billing.easybatch` |
| artifactId | `easybatch-jdbc` |
| version | `5.3.0-jakarta` |
| packaging | `jar` |

## Build

`mvn install -pl 
