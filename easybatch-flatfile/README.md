# easybatch-flatfile

Easy Batch flat file processing module

Child of `com.billing.easybatch:easybatch`.

## Maven coordinates

| Field | Value |
|---|---|
| groupId | `com.billing.easybatch` |
| artifactId | `easybatch-flatfile` |
| version | `5.3.0-jakarta` |
| packaging | `jar` |

## Build

`mvn install -pl 
